version: '3'

services:
  elasticsearch:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./elasticsearch/data:/usr/share/elasticsearch/data
    environment:
      ES_JAVA_OPTS: "-Xmx2048m -Xms2048m"
    ports:
      - 9200:9200
      - 9300:9300

  kibana:
    image:
      "kibana:7.6.0"
    volumes:
      - ./config/kibana.yml:/usr/share/kibana/config/kibana.yml
    ports:
      - 5601:5601

  logstash:
    image:
      "logstash:7.6.0"
    volumes:
      - ./config/logstash.conf:/usr/share/logstash/config/logstash.conf
    ports:
      - 5000:5000
      - 9600:9600
    entrypoint: ["/usr/local/bin/docker-entrypoint", "-f", "/usr/share/logstash/config/logstash.conf"]